public class April25thLabPart2
{
	public static April25thLabPart2(int[] array, int numberUsed)
	{
		int number;
		for(numberUsed = 0; number < numberUsed; numberUsed++)
		{
			array[number] = {};
		}
		
		indexofSmallest(0, array, numberUsed);
		
		
	}
	
	private int indexofSmallest(int startIndex, int[] a, int numberUsed) //finds smallest element of array
	{
		int indexOfSmallestNumber;
		int indexOfMin = startIndex;
		int index;
		for(index = startIndex + 1; index < numberUsed; index++)
		if(a[index]
		
		return indexOfSmallestNumber;
	}
}
